@model BookReservationSystem.Domain.LibraryDto
@{
    ViewData["Title"] = "Edit Library";
}

@using (Html.BeginForm("Edit", "Library", FormMethod.Post))
{
    @Html.HiddenFor(x => x.Id)
    @await Html.PartialAsync("_Form")
}
